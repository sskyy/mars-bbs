'use strict';

module.exports = function ( galaxies ) {
  return {
    /*
    some client event listener, example
    'user.signup'  : {
      fn : function(){
        window.location.href = '/'
      },
      last : true
    }
    */
  }
};

